extends Object
class_name GamePalette

# Bright modern-city palette for a GTA-inspired 2D open-world feel.
# Flat shapes keep GPU and RAM cost low on first-gen Intel i3 class machines.

const SKY_TOP = Color(0.38, 0.72, 1.0)
const SKY_MID = Color(0.58, 0.84, 1.0)
const SKY_BOTTOM = Color(0.86, 0.94, 1.0)

const SUNLIGHT = Color(1.0, 0.86, 0.32, 0.22)
const GRASS_A = Color(0.24, 0.68, 0.27)
const GRASS_B = Color(0.18, 0.58, 0.22)
const ROAD = Color(0.28, 0.30, 0.32)
const ROAD_EDGE = Color(0.18, 0.19, 0.20)
const SIDEWALK = Color(0.68, 0.70, 0.70)
const SIDEWALK_LINE = Color(0.54, 0.56, 0.56)
const LANE_MARKING = Color(1.0, 0.88, 0.22)
const CROSSWALK = Color(0.92, 0.93, 0.91)

const BUILDING_BLUE = Color(0.18, 0.48, 0.78)
const BUILDING_TAN = Color(0.78, 0.58, 0.36)
const BUILDING_RED = Color(0.78, 0.28, 0.22)
const BUILDING_WHITE = Color(0.86, 0.86, 0.80)
const WINDOW = Color(0.78, 0.92, 1.0, 0.85)
const SHOP_AWNING = Color(0.95, 0.18, 0.25)

const TREE_TRUNK = Color(0.45, 0.26, 0.12)
const TREE_LEAF = Color(0.10, 0.56, 0.18)
const LAMP_POST = Color(0.18, 0.20, 0.22)
const LAMP_GLOW = Color(1.0, 0.92, 0.42, 0.28)

const PLAYER_BODY = Color(0.12, 0.30, 0.88)
const PLAYER_SHIRT = Color(0.95, 0.95, 0.92)
const PLAYER_ACCENT = Color(0.95, 0.76, 0.20)
const PLAYER_SKIN = Color(0.78, 0.50, 0.32)
const PLAYER_BLADE = Color(0.25, 0.36, 0.46)

const VEHICLE_RED = Color(0.92, 0.16, 0.12)
const VEHICLE_BLUE = Color(0.08, 0.26, 0.82)
const VEHICLE_GLASS = Color(0.55, 0.84, 1.0, 0.92)

const HUD_PANEL = Color(0.98, 0.98, 0.95, 0.86)
const HUD_BORDER = Color(0.08, 0.16, 0.24, 0.75)
const HP_FILL = Color(0.08, 0.78, 0.34)
const MONEY_GREEN = Color(0.05, 0.55, 0.18)
const MISSION_TEXT = Color(0.08, 0.11, 0.16)
